[buildout]
extends =
    buildout.d/staging.cfg
    buildout.d/test.cfg

newest = false

parts +=
    jenkins-test
    jenkins-test-coverage


[instance-settings]
user = admin:admin

[users]
main =
cache =
transform =
balancer =
zope =
supervisor =
